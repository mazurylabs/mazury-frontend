import { Layout } from 'components';

const Jobs = () => {
  return (
    <Layout variant="plain">
      <div>
        <h1>This is the jobs page</h1>
      </div>
    </Layout>
  );
};

export default Jobs;
